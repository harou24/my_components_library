export type CardProps = {
    isBgImage?: boolean,
    bgColor?: string,
    bg?: string,
    bgImage?: string,
    borderRadius?: string,
    boxShadow?: string,
    height: string,
    width: string,
    opacity?: string,
    isCursorPointer?: boolean,
    element?: JSX.Element,
}