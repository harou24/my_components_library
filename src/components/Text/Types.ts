export type TextProps = {
    title?: string,
    subtitle?: string,
    paragraph?: string,
}

export type TextStyleProps = {
    color?: string,
    fontSize?: string,
    letterSpacing?: string,
    lineHeight?: string,
    fontWeight?: string,
    upperCase?: boolean
}